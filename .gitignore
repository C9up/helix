node_modules/
target/
# …including when it is a symlink pointing the build output elsewhere,
# which `target/` does not match.
target
dist/
/coverage/
*.node
.env
.env.local
.DS_Store
*.log
.turbo/

# Throwaway fixtures used to verify a change by hand — never source.
.tmp-*/
